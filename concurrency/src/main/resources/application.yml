spring:
  application:
    name: ticketing-concurrency
  datasource:
    url: jdbc:postgresql://localhost:5433/ticketing
    username: ticketing
    password: ticketing
    hikari:
      maximum-pool-size: 10
      connection-timeout: 3000
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        jdbc.batch_size: 50
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 28081

management:
  endpoints:
    web:
      exposure:
        include: health, info, metrics, prometheus
  metrics:
    tags:
      stage: concurrency
    distribution:
      percentiles-histogram:
        http.server.requests: true
      percentiles:
        http.server.requests: 0.5, 0.95, 0.99

logging:
  level:
    com.dongwoo.ticketing: info
    org.hibernate.SQL: info

ticketing:
  pgmock:
    # latency 평균 120ms ±60ms (분포)
    # 출처: Stripe production median ~120ms (Toss 공식 SLA 미공개)
    # https://medium.com/@warstories/the-stripe-latency-post-mortem-every-engineer-should-read-before-launching-their-api-6514411772f8
    latency-mean-ms: 120
    latency-jitter-ms: 60
    callback-url: http://localhost:28081/payments/callback
