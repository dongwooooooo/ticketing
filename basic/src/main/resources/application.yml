spring:
  application:
    name: ticketing-basic
  datasource:
    url: jdbc:postgresql://localhost:5432/ticketing
    username: ticketing
    password: ticketing
    hikari:
      maximum-pool-size: 10
      connection-timeout: 3000
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        jdbc.batch_size: 50
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 28080

management:
  endpoints:
    web:
      exposure:
        include: health, info, metrics, prometheus
  metrics:
    tags:
      stage: basic
    distribution:
      percentiles-histogram:
        http.server.requests: true
      percentiles:
        http.server.requests: 0.5, 0.95, 0.99

logging:
  level:
    com.dongwoo.ticketing: info
    org.hibernate.SQL: info

ticketing:
  pgmock:
    latency-mean-ms: 120
    latency-jitter-ms: 60
    callback-url: http://localhost:28080/payments/callback
